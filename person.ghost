class Person {
    name := ""
    age := 0

    function constructor(name, age) {
        this.name := name
        this.age := age
    }

    function greet() {
        print("Hello my name is %s".format(this.name))
    }
}